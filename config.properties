# Chemins
data.dir=data
domains.dir=data/domains
dataset.dir=data/dataset
models.ml.dir=data/models-ml
transformations.dir=data/transformations
reports.dir=data/reports

# API Flask
flask.host=localhost
flask.port=5000
flask.url=http://localhost:5000

# Seuils ML
ml.accuracy.threshold=0.85
ml.confidence.threshold=0.80
ml.similarity.threshold=0.60

# Dataset
dataset.min.rows=4000
dataset.min.per.label=200

# Migration
migration.success.threshold=0.90